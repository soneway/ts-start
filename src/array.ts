// 内置对象
function sum() {
  let args = arguments;
}
let divEl = document.querySelector('div')?.childNodes;


// 声明
let $div = $('div');
let $p = jQuery('p');
$.ajax('', {});

let cat = new Animal();

let dirs = [Direction.top, Direction.right];

request.ajax('', {});
